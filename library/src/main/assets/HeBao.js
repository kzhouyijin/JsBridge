//notation: js file can only use this kind of comments
//since comments will cause error when use in webview.loadurl,
//comments will be remove by java use regexp
(function() {
   function HeBao()
   {
   }

   HeBao.doLogin(var name)
   {
    alert(name);
   }
})();
